# 分销提现审批 — `/api/v1/admin/marketing`

**GET** `/api/v1/admin/marketing/withdrawals?status=X` — 提现申请列表

**PUT** `/api/v1/admin/marketing/withdrawals/{withdrawId}/approve` — 审批 `{ "result": 1, "remark": "" }`

---

