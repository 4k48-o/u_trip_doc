# 消息模板 — `/api/v1/admin/notifications`

**GET/POST/PUT** `/api/v1/admin/message-templates` — 模板 CRUD（短信/邮件/微信）

**POST** `/api/v1/admin/notifications/batch` — 批量发送 `{ "templateId": "T001", "targetType": "role/merchant/user", "targetIds": [...], "params": {...} }`

---

