数据库设计审查报告

  ---
  🔴 严重（P0）—— 直接影响正确性或数据安全

  1. enums.md 与 order_main.status 枚举值完全冲突

  ┌────────────────────────┬────────────────────────────────────────┬──────────────────┐
  │          来源          │                   值                   │       语义       │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ enums.md / OrderStatus │ 0=待支付, 1=已支付, 2=配送中, 3=已签收 │ 电商通用模板     │
  ├────────────────────────┼────────────────────────────────────────┼──────────────────┤
  │ order_main.status      │ 1=待支付, 2=已支付, 3=已核销, 7=已关闭 │ 景区票务实际状态 │
  └────────────────────────┴────────────────────────────────────────┴──────────────────┘

  两份文档完全不匹配，代码生成或前端接入时必然出错。enums.md 里的 SHIPPING/DELIVERED 对票务毫无意义。需要以 order_main 为准重写 enums.md，并补全所有业务状态定义。

  ---
  2. ticket_visitor.age 类型错误（数据截断风险）

  age tinyint(1)   ← 在 ORM 框架中通常映射为 boolean (0/1)

  tinyint(1) 是 MySQL 布尔的惯用类型，大多数 Java/MyBatis 框架会自动映射为 Boolean。年龄 18 岁会被截断为 1。应改为 tinyint(3) UNSIGNED 或 smallint。

  ---
  3. 所有业务表写入同一 jeecg-boot 库——与微服务架构矛盾

  architecture.md 明确指出：

  ▎ "数据分析分离 → 独立 analytics-service + 独立 DB"

  而 database.md §3 开头说：

  ▎ "以下表需在 JEECG Boot 基础上新增，使用同一数据库 jeecg-boot"

  87 张业务表全部耦合在一个库里，每个服务直接读写其他服务的表，彻底打破了微服务边界。需要按服务拆分库（ticket_db, order_db, product_db...），服务间只通过 API 或消息通信。

  ---
  4. order_main 缺少 expire_time（订单无法自动超时取消）
  
  ticket_reservation 有 expire_time，但转为正式订单后 order_main 没有这个字段。景区票务的核心约束是"N 分钟内未支付自动取消并释放库存"，没有 expire_time 就无法触发 XXL-JOB 超时任务，也无法在网关层做早期拦截。

  ---
  5. 字符集排序规则前后矛盾

  - database.md §1：utf8mb4_general_ci
  - tech-stack.md §4：utf8mb4_unicode_ci

  unicode_ci 能正确处理多语言字符（重音、东南亚文字），对景区国际游客场景是必须的。两处不一致必须统一，应采用 utf8mb4_unicode_ci。

  ---
  🟠 重要（P1）—— 功能缺失或设计逻辑漏洞
  
  6. product_combo 缺少 spu_id

  组合产品是独立实体，没有关联 product_spu，导致：
  - 无法给组合产品配置价格日历（product_price_calendar）
  - 无法配置渠道定价（product_channel_price）
  - 无法做多语言内容（product_language）
  - 组合产品无法出现在商品搜索/列表中
  
  建议：将 product_combo 设计为 SPU 的一种，或在 SPU 表中增加 combo_id 外联。

  ---
  7. product_combo.total_price 与明细价格产生双写
  
  product_combo.total_price 与 product_combo_item.sell_price × quantity 之和语义重叠。当子项价格更新后，total_price 不会自动同步，清分金额会出现偏差。建议删除 total_price，由程序实时计算，或作为缓存字段明确标注计算逻辑。

  ---
  8. 库存双源——product_price_calendar.stock 与 ticket_stock 同时存在
  
  product_price_calendar 有 stock 字段，ticket_stock 也按 (product_id, visit_date, time_slot, channel) 维度存库存。两处哪个是权威源？减库存时写哪张表？不明确的双源库存在高并发下必然超卖。

  ---
  9. 缺少游客用户表
  
  sys_user 是后台系统用户表（职员/管理员），景区 C 端游客通过微信/支付宝登录，其用户数据存在哪里？sys_third_account 只是第三方账号绑定，没有游客专属的档案表（历史订单、收藏、消费习惯等）。ticket_reservation 和 order_main 里的
  user_id 在游客匿名下单时指向什么？

  ---
  10. merchant_info 和 merchant_shop 缺少 area_id
  
  文档背景是慕田峪景区，如果系统将来扩展到多景区（或同一集团下多个景区），商户和店铺没有景区归属字段，数据将无法隔离。即使当前单景区，物理门店的位置信息也应有关联。

  ---
  11. inv_inbound.unique_code 设计违背"一物一码"
  
  表注释写"一物一码"，但 unique_code 是入库单级别的字段，而 quantity > 1 时一张入库单有多件商品，每件商品理应有独立唯一码。应拆出 inv_inbound_item 明细表，每行对应一件商品的唯一码。

  ---
  12. content_audio_guide.scenic_spot 存景点名称字符串

  scenic_spot varchar(100)   ← "慕田峪1号台"

  应为 scenic_spot_id varchar(32)，关联 scenic_spot.id。若景点改名，所有音频讲解记录都需批量更新，严重违反范式。

  ---
  🟡 中等（P2）—— 影响查询性能或运营准确性
  
  13. season_config.time_slots 用 JSON 文本存时段+容量

  [{"slot":"08:00-10:00","max":500},{"slot":"10:00-12:00","max":800}]

  查询某个时段的剩余容量、修改单个时段上限，都需要在应用层解析 JSON。建议拆为 season_config_slot 子表，每个时段一行，可独立查询和更新。

  ---
  14. member_info.tags 用逗号分隔字符串

  tags varchar(500)   ← "亲子游,本地人,历史爱好者"

  按标签查询只能用 LIKE '%亲子游%'，无法走索引。应改为 JSON 数组，或建 member_tag 关联表。

  ---
  15. member_points_log 缺少积分到期日期
  
  积分没有有效期字段，积分永不过期既不符合会员运营需求，也会造成高额未兑现负债累积。需在明细行增加 expire_date。

  ---
  16. marketing_coupon_use 不存储用户券到期时间
  
  到期时间需要运行时计算：receive_time + effective_days。在验券、券过期批量处理等场景，实时计算代价高，且无法直接在 SQL 中按到期时间索引过滤。建议落库 expire_time。

  ---
  17. merchant_commission 缺少唯一约束
  
  (merchant_id, category_code) 上没有唯一索引，同一商户同一业态可能存在多条有效佣金记录，清分时拿到哪条全凭查询顺序。

  ---
  18. settlement_nc_voucher.nc_biz_id 缺少唯一约束
  
  用友 NC 的业务 ID 代表凭证唯一性，若重复同步会在 NC 产生重复财务凭证。应加 uk_nc_biz_id。

  ---
  19. ticket_stock 无防超卖约束
  
  used_stock + frozen_stock > total_stock 在数据库层面没有 CHECK 约束（MySQL 8.0 支持）也没有 available_stock 字段。高并发抢票场景下仅靠应用层乐观锁容易出现竞态。建议至少加 CHECK (used_stock + frozen_stock <= total_stock) 或用
  Redis Lua 脚本做原子扣减，并在文档中明确说明。

  ---
  20. 高频 IoT 表使用 varchar(32) 主键
  
  traffic_store_flow（实时客流）和 vehicle_gps_log（GPS 定位）每分钟可能产生大量写入，雪花 ID varchar(32) 作为聚集索引会导致页分裂（B+ 树随机插入）。这两张表建议：
  - 主键改为 BIGINT AUTO_INCREMENT
  - 按月/日分区（PARTITION BY RANGE）
  - 历史数据定期归档到冷存储

  ---
  🔵 较低（P3）—— 设计不完整，待补充
  
  21. biz_shop_lease 引用 shop_id 但缺少物业空间表

  商铺租约的 shop_id 指向的是 merchant_shop（商户运营的店铺），但租约的主体应该是景区拥有的物业空间（铺位号、面积、位置），不是商户店铺。缺少 biz_property_space 物业空间表。

  22. inv_outbound 缺少 ref_order_id

  销售型出库单（outbound_type=sale）没有关联 POS 收银单，收银流水和库存变动无法对账。

  23. order_payment 缺少标准审计字段

  order_payment 没有 create_by, update_by, update_time, del_flag，违反第 §1 节全局约定。

  24. order_main 缺少 agent_id

  旅行社下的订单（channel=agent）只有 channel_order_no，没有 agent_id 字段，无法从订单直接关联旅行社进行账期统计和佣金清分。

  25. ota_reconciliation 只有汇总，无行级差异明细

  对账差异只记录了 diff_count 和 diff_amount，运营人员无法逐单核查哪些订单有差异，需要补充 ota_reconciliation_item 差异明细表。

  26. ota_product_mapping 无版本历史

  OTA 商品映射变更后只更新当前记录，历史映射关系丢失，历史订单对账时无法还原当时的映射状态。

  ---
  汇总
  
  ┌────────────┬──────┬─────────────────────────────────────────────────────────────────┐
  │    级别    │ 数量 │                            典型问题                             │
  ├────────────┼──────┼─────────────────────────────────────────────────────────────────┤
  │ 🔴 P0 严重 │ 5    │ enums冲突、age类型错误、微服务共库、缺expire_time、排序规则矛盾 │
  ├────────────┼──────┼─────────────────────────────────────────────────────────────────┤
  │ 🟠 P1 重要 │ 7    │ combo无SPU、库存双源、缺游客用户表、一物一码设计错误            │
  ├────────────┼──────┼─────────────────────────────────────────────────────────────────┤
  │ 🟡 P2 中等 │ 8    │ JSON字段滥用、积分无到期、IoT表性能隐患                         │
  ├────────────┼──────┼─────────────────────────────────────────────────────────────────┤
  │ 🔵 P3 较低 │ 6    │ 字段缺失、审计字段遗漏、缺对账明细                              │
  └────────────┴──────┴─────────────────────────────────────────────────────────────────┘

  优先建议：P0 全部在设计阶段修复，P1 在第一个迭代启动前完成，否则后期改动成本极高（需数据迁移）。